$schema: https://git.drupalcode.org/project/drupal/-/raw/10.1.x/core/modules/sdc/src/metadata.schema.json
name: Page Header
props:
  type: object
  properties:
    logo:
      type: [string, 'null']
      title: Site logo
      description: Sitewide logo
    navigation:
      type: [array, 'null']
      title: Main menu
      description: The main navigation for the site
    search:
      type: [string, 'null']
      title: Search box
      description: Search box for input the search term
    extra:
      type: [string, 'null']
      title: Extra items
      description: Space for various, optional, items e.g. language switcher, mobile toggle…
