{% for item in items %}
  {{ include ('gov_cz:gov-chip', {
    chip_url: '/akce?f[0]=event_type:' ~ item.content['#entity'].tid.value,
    chip_content: item.content['#title'],
    })
  }}
{% endfor %}
